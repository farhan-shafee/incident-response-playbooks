name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - repo-polish

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repository validation
        run: make validate
